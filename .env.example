# Configuration OSRM
# Pour dev local avec OSRM local : décommenter et lancer avec --profile local-osrm
# OSRM_BASE_URL=http://osrm:5000

# Pour dev local avec API publique (par défaut si non défini)
# OSRM_BASE_URL=https://router.project-osrm.org

# La CI utilise automatiquement l'API publique OSRM
